<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.livedata_viewmodel_databinding">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveData_ViewModel_DataBinding">
        <activity android:name=".mvvm2.StringActivity"></activity>
        <activity android:name=".mvvm1.VmShareActivity" />
        <activity android:name=".mvvm1.MainActivity2" />
        <activity android:name=".mvvm1.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>